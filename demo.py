from cognisync.audit import AuditLog
from cognisync.cli import main

# The canonical demo entry point remains `python -m cognisync`.
# This file exists so reviewers can discover a root-level, obvious demo target.

if __name__ == "__main__":
    main()
